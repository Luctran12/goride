-- Purpose: safely reverse apply.sql when rollback is approved.
-- Replace this template with release-specific rollback SQL.

BEGIN;

-- Example:
-- DROP INDEX IF EXISTS idx_example_created_at;

COMMIT;
