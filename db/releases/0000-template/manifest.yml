release_id: 0000-template
title: Replace with release title
author: Replace with reviewer/owner
created_at: 2026-06-30
risk: low
transactional: true
requires_downtime: false
applies_after: []
related_commit: pending
environments:
  - staging
  - production
objects:
  - schema_or_table_name
pre_deploy_checks:
  - Run precheck.sql and confirm it returns the expected current state.
deploy_steps:
  - Run apply.sql inside a reviewed deployment window.
post_deploy_checks:
  - Run verify.sql and archive the output with the release notes.
rollback_plan:
  - Run rollback.sql only after confirming the rollback impact.
notes:
  - Replace this template before review.
