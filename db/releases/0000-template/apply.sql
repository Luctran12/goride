-- Purpose: apply the reviewed database change.
-- Replace this template with release-specific SQL.

BEGIN;

-- Example:
-- CREATE INDEX IF NOT EXISTS idx_example_created_at ON example_table(created_at);

COMMIT;
