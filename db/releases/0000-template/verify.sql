-- Purpose: prove that apply.sql completed successfully.
-- Replace this template with release-specific checks.

SELECT current_database() AS database_name;

-- Example:
-- SELECT to_regclass('public.idx_example_created_at') IS NOT NULL AS index_exists;
